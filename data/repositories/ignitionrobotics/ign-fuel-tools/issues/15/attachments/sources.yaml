---
# The list of servers.
servers:
  osrf:
    url: http://localhost:8080
    server_type: girder
    api_key: redacted
    types:
      models: osrf/models
      worlds: osrf/worlds

  osrf_public:
    url: http://data.ignitionfuel.org
    server_type: girder
    api_key: redacted
    types:
      models: osrf/models
      worlds: osrf/worlds

  # Another server.
  # my_server: https://myserver
  #   types:
  #     turtles: my_collection/turtles

# Other configuration options.
install_dir: /home/caguero/.ignition/fuel/
